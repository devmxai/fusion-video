# Fusion Video Engine Spec

The canonical engine specification lives in `docs/ENGINE_SPEC.md`.

This top-level file exists as a stable entry point for future threads and
handoffs so the engine spec can be opened from the project root.
